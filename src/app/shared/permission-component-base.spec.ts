import { PermissionComponentBase } from './permission-component-base';

describe('PermissionComponentBase', () => {
  it('should create an instance', () => {
    expect(new PermissionComponentBase()).toBeTruthy();
  });
});
